automatic_id 1
script = jumper.lua
inputs = [ tick ]
# w for wish - direction to where jumper is going to
# x and y is an old position for later interpolation if needed
# f for flags, to skip ticks and such
vars = { u8 f = 0 u8 w = 0 i16 x = 0 i16 y = 0 u32 t = 0 }
texture = jumper.png
offset_x_controller = x
offset_y_controller = y
interp_timestamp_controller = t
interp_takes = 100
